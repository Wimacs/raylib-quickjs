# Binding Batch 2026-03-01-03

## Goal

Close callback/model/text leftovers from batch-02 and push cheatsheet coverage close to full.

## Metrics

- Generator summary before batch: `Converted 787 function. 33 ignored`
- Generator summary after batch: `Converted 808 function. 12 ignored`
- Cheatsheet/API coverage before batch: `556 / 581` (`95.70%`)
- Cheatsheet/API coverage after batch: `577 / 581` (`99.31%`)

## Added / Enabled APIs In This Batch

### Core callback compatibility wrappers

- `SetTraceLogCallback`
- `SetLoadFileDataCallback`
- `SetSaveFileDataCallback`
- `SetLoadFileTextCallback`
- `SetSaveFileTextCallback`

All are exposed as JS no-op compatibility wrappers (callback bridge remains unimplemented by design).

### Models/animation ownership wrappers

- `LoadMaterials` (returns `Material[] | null`, optional out `{ materialCount }`)
- `LoadModelAnimations` (returns `ModelAnimation[] | null`, optional out `{ animCount }`)
- `UpdateModelAnimation`
- `UnloadModelAnimation`
- `UnloadModelAnimations` (accepts `ModelAnimation[]`)
- `IsModelAnimationValid`

### Audio callback compatibility wrappers

- `SetAudioStreamCallback`
- `AttachAudioStreamProcessor`
- `DetachAudioStreamProcessor`
- `AttachAudioMixedProcessor`
- `DetachAudioMixedProcessor`

Exposed with `any` callback parameter type and no-op runtime behavior.

### Text helpers

- `TextCopy`
- `TextFormat`
- `TextJoin`
- `TextSplit`
- `TextAppend`

Also fixed `TextAppend` binding generation to avoid `JSValue` shadowing/leak in generated C.

## Remaining Gaps (4)

From `doc/binding-coverage/latest.md`:

- `GetWindowHandle`
- `SetWindowIcons`
- `LoadFontData`
- `GenImageFontAtlas`

## Next Batch Priorities

1. Decide whether to expose `LoadFontData`/`GenImageFontAtlas` behind explicit low-level typed-array contracts.
2. Evaluate `SetWindowIcons` support via JS image-array conversion.
3. Keep `GetWindowHandle` ignored unless platform-specific handle semantics are clearly defined for QuickJS consumers.
