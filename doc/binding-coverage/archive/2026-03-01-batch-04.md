# Binding Batch 2026-03-01-04

## Goal

Close the final cheatsheet-visible gaps and complete full API coverage in declarations.

## Metrics

- Generator summary before batch: `Converted 808 function. 12 ignored`
- Generator summary after batch: `Converted 812 function. 8 ignored`
- Cheatsheet/API coverage before batch: `577 / 581` (`99.31%`)
- Cheatsheet/API coverage after batch: `581 / 581` (`100.00%`)

## Added / Enabled APIs In This Batch

### Core/window

- `SetWindowIcons` (accepts `Image[]`, internally flattens to native array)
- `GetWindowHandle` (compatibility export returning `null` in JS)

### Text/font

- `LoadFontData`
  - JS API: `loadFontData(fileData, fontSize, codepoints, codepointCount, type)`
  - returns `GlyphInfo[] | null`
  - handles data-size internally from `ArrayBuffer`
- `GenImageFontAtlas`
  - JS API: `genImageFontAtlas(glyphs, fontSize, padding, packMethod)`
  - derives `glyphCount` from array length
  - uses native `GenImageFontAtlas` and frees temporary native buffers

## Remaining Gaps

- Cheatsheet-visible functions missing from declarations: **0**

## Explicit Ignores Remaining (8)

- `GuiGetIcons`
- `GuiListViewEx`
- `GuiLoadIcons`
- `GuiTabBar`
- `MatrixToFloatV`
- `QuaternionToAxisAngle`
- `Vector3OrthoNormalize`
- `Vector3ToFloatV`

## Next Batch Priorities

1. Decide if the 8 explicit ignores should remain intentionally unsupported or move into an `unsafe` namespace.
2. Add runtime tests for new complex wrappers (`SetWindowIcons`, `LoadFontData`, `GenImageFontAtlas`) to catch ownership regressions.
